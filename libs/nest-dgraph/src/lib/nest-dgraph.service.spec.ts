import {Test} from '@nestjs/testing';
// import {NestDgraphService} from './nest-dgraph.service';

describe('NestDgraphService', () => {
  // let service: NestDgraphService;

  // beforeEach(async () => {
  //   const module = await Test.createTestingModule({
  //     providers: [NestDgraphService],
  //   }).compile();
  //
  //   service = module.get(NestDgraphService);
  // });
    beforeEach(async () => {
        // Dud code to make it compile
      await Test.createTestingModule({}).compile();
    });
  //
  // it('should be defined', () => {
  //   expect(service).toBeTruthy();
  // });
    it('needs tests written', () => {
        expect(true)
    })
});
